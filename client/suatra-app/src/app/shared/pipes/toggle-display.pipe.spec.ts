import { ToggleDisplayPipe } from './toggle-display.pipe';

describe('ToggleDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new ToggleDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
