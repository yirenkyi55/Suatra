import { ObjectAccessorPipe } from './object-accessor.pipe';

describe('ObjectAccessorPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectAccessorPipe();
    expect(pipe).toBeTruthy();
  });
});
